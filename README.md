# LexInvo POC

Azure JSON → BT Store → Corrections Report → EN16931 BASIC Output

## Quick start (placeholder)
- Add your Azure invoice JSON to `input/azure_invoice.json`.
- Run the POC entrypoint (to be implemented) to produce outputs in `output/`.

## Folder map
- `config/` mapping + BT registry
- `data/` codelists / mapping tables
- `input/` sample inputs
- `output/` generated outputs
- `src/lexinvo/` core implementation
